{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "noEmit": true,
    // `node` is needed by the test files, which run under Node's native runner
    // even though the code under test targets the browser.
    "types": ["vite/client", "node"]
  },
  "include": ["src/**/*"]
}
