# The database. Without it, the server refuses to start rather than serve badly.
DATABASE_URL=postgres://atem:atem@localhost:5432/atem

# The token signing key. There is no fallback value:
#   openssl rand -base64 32
JWT_SECRET=

# The listening interface and port. 127.0.0.1 by default: an instance is never
# exposed to the network unless someone decided so.
ATEM_HOST=127.0.0.1
ATEM_PORT=3000

# PostgreSQL connection pool size.
ATEM_DB_POOL=10

# Authentication attempt ceilings, per hour and per address. The defaults
# (5 sign-ups, 10 sign-ins) target home use. A shop signing up twenty players on
# a tournament evening, all behind the same address, needs to loosen them.
# ATEM_REGISTER_ATTEMPTS_MAX=5
# ATEM_LOGIN_ATTEMPTS_MAX=10

# The proxies whose X-Forwarded-For may be believed — addresses or CIDR ranges,
# comma-separated. Behind a reverse proxy this is NOT optional: without it every
# request looks like it comes from the proxy, the whole instance shares one
# rate-limit bucket, and ten failed sign-ins lock everyone out for fifteen
# minutes. compose.yaml sets it for the bundled deployment.
# Leave it empty when nothing sits in front: the header is then ignored, which
# is what keeps a stranger from forging an address per request.
# ATEM_TRUSTED_PROXIES=10.89.42.0/24

# Where card images are stored once downloaded. Default: data/media at the
# repository root. Keep it on persistent storage — every lost image is pulled
# again from YGOPRODeck, which blacklists repeated pulls.
# ATEM_MEDIA_DIR=

# Free space always left on the media disk, in bytes (default 200 MB). Below it,
# no new image is downloaded, so PostgreSQL never runs out of room first.
# ATEM_DISK_RESERVE_BYTES=
